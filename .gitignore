### --- JAVA & COMPILAÇÃO --- ###
*.class
*.log
*.ctxt
# Arquivos de log de erro da JVM
hs_err_pid*
replay_pid*

### --- BUILD TOOLS (Maven/Gradle) --- ###
target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties

### --- INTELLIJ IDEA --- ###
.idea/
*.iml
*.iws
*.ipr
out/
# Se você quiser versionar o estilo de código do time, 
# pode remover a linha .idea/ acima e usar regras mais finas.

### --- DOCKER --- ###
# Não ignore o Dockerfile ou docker-compose.yml! 
# Ignore apenas arquivos de dados ou estados:
.docker/
# Se você usa volumes locais para persistência de DB no Docker:
**/data/
**/logs/

### --- CONFIGURAÇÕES & SEGURANÇA (IMPORTANTE) --- ###
# Ignore arquivos que contenham senhas ou chaves de API
.env
*.properties
*.yaml
*.yml
# Dica: Deixe apenas o 'application.yml.example' no Git
!src/main/resources/application.properties
!src/main/resources/application.yml

### --- LOGGERS --- ###
# Ignora logs gerados em qualquer pasta (ex: logs do Logback ou Log4j)
logs/
*.log
*.log.*

### --- OS TEMPORARY FILES --- ###
.DS_Store
Thumbs.db